language: crystal
sudo: required
dist: trusty
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libnlopt0
  - sudo ln -s /usr/lib/x86_64-linux-gnu/libnlopt.so.0 /usr/lib/libnlopt.so
